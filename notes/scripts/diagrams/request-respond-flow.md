# HTTP Request-Response Flow

This file describes the basic flow of an HTTP request and response:

1. **Client sends request** to server (GET, POST, etc.).
2. **Server processes** the request.
3. **Server sends response** back to client (HTML, JSON, etc.).

## 🔁 Example Flow
